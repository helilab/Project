% Compute cost at given vector
function cost = mynonlcon(z, nx, nu, N, q1, q2)
    tempCost = 0;
    for i = 0:N-1
        % lambda
        tempCost = tempCost + z(1 + N*nx:
        % pitch input
        
        % elevation input
end